with uniqueSumPackage; use uniqueSumPackage;

procedure Main is
   inputs : input_args;
begin
   inputs := get_inputs;
   -- unique_sum(inputs(0), inputs(1), inputs(2));
   unique_sum(inputs);
end Main;
